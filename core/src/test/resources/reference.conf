
tasks.cache.enabled = true

tasks.fileservice.writeFileHistories = true

tasks.askInterval = 500 ms

# The shipped default is tuned to keep writes off a remote queue state backend
# and is far too slow for tests that wait on launcher crash detection.
tasks.failuredetector.heartbeat-interval = 200 ms